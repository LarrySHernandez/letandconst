QUIZ

Q1)What is the difference between var and let?

A1)Var is able to be re-declared and re-assigned. Var is also able to mutate and change variables in the window. Var is 
also declared first before initialization. Let can only re-assign.

Q2)What is the difference between var and const?


A2) Var is able to be re-declared and re-assigned. Const cannot. 


Q3)What is the difference between let and const?

A3)Let is able to be re-assigned. Const cannot.


Q4)What is hoisting?

A4) Hoisitng is when either a variable or function is hoisted up to the top of the scope and declared prior to being assigned 
and initialization.

